#include <iostream>
#include <string.h>
using namespace std;
int main()
{
    int p,k,l=1;   
    cin >> p >> k;
    char a[p*20+1]
    for (int i = 0; i < p; i++)
    {
        for (int i = 0; i < 20; i++)
        {
            cin >> a[l++];
        }
        cout << endl;
    }
    int s;
    cin >> s;
    char *b[s+1];
    for (int i = 1; i <= s; i++)
    {
        cin >> b[i];
    }
    
    
	return 0;
}