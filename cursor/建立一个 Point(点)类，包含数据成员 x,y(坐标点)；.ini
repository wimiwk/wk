建立一个 Point(点)类，包含数据成员 x,y(坐标点)；
（1）以 Point 为基类，派生出一个 Circle(圆)类，增加数据成员(半径)，基类的成员
表示圆心；
（2）编写上述两类中的构造、析构函数及必要的成员函数
（3）重载运算符“<<”（输出运算符），实现自定义的输入和输出
（4）定义友元函数 int locate，判断点 p 在圆 c 上、圆 c 内或圆 c 外，返回值<0 圆
内，==0 圆上，>0 圆外；
（5）重载关系运算符（6 种）运算符，使之能够按圆的面积比较两个圆的大小；