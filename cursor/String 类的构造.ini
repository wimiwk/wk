String 类的构造
题目描述：
写一个能处理字符串的类，其数据成员如下所示：
class String
{
public:
...//需要的成员函数（若需要的话，声明友元函数）
private:
char *p; //指向存储的字符串
int len; //记录字符串的长度
};
请构造 String 类的加、减运算。其中，s1 + s2 将两个字符串的连接起来；s1 - s2
是将 s1 的尾部空格和 s2 的前导空格去除后的连接。
提示：有指针成员，设计时要注意。